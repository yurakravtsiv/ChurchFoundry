---
description: Нові поля предмета інвентаря треба додавати до текстового пошуку
globs: ["src/types/inventory.ts", "src/lib/inventorySearch.ts", "src/lib/inventoryLabels.ts", "src/pages/InventoryPage.tsx", "src/components/inventory/**"]
alwaysApply: false
---

# Покриття полів у пошуку інвентаря

Текстовий пошук на сторінці інвентаря має знаходити предмети за **усіма**
користувацькими / текстовими значеннями предмета.

Джерело правди для списку пошукових значень:
`src/lib/inventorySearch.ts` (`getInventoryItemSearchableValues`).

## Обов'язково при зміні моделі

Коли додаєш нове поле до `InventoryItem` (або нове відображуване значення,
похідне від предмета — назва категорії/локації, локалізований enum тощо):

1. Додай відповідне значення в `getInventoryItemSearchableValues`.
2. Для enum / статусів шукай за **локалізованим** лейблом через `t(...)`
   (див. `src/lib/inventoryLabels.ts`), а не лише за сирим ключем на кшталт
   `needs_repair`.
3. Не додавай бінарні / технічні дані (base64 фото, внутрішні id фото),
   якщо користувач їх ніколи не вводить у пошук.
4. `inventoryNumberId` **не** додавати до пошуку — це виняток за вимогою продукту.

## Перевірка

Після зміни моделі або форми предмета переконайся, що типовий текст з UI
(наприклад «Потребує ремонту», «Позичено») знаходиться через рядок пошуку.
